<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajouter un Médecin</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f3f3f5;
            color: #333;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            color: #6a1b9a;  /* Violet color for header */
            margin-top: 50px;
            font-size: 2.5em;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 10px;
            font-weight: bold;
        }

        input[type="text"], input[type="email"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        button {
            padding: 10px 15px;
            background-color: #6a1b9a; /* Violet */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            transition: all 0.3s ease-in-out;
        }

        button:hover {
            opacity: 0.9;
        }

        .back-button {
            background-color: #1976d2; /* Blue */
            margin-top: 10px;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Ajouter un Médecin</h1>
        <!-- Form directly submits data to /ajouteMedecin -->
        <form action="/api/medecins/ajouteMedecin" method="POST">
            <label for="nom">Nom:</label>
            <input type="text" id="nom" name="nom" required>

            <label for="specialite">Spécialité:</label>
            <input type="text" id="specialite" name="specialite" required>

            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required>

            <label for="adresse">Adresse:</label>
            <input type="text" id="adresse" name="adresse" required>

            <button type="submit">Ajouter Médecin</button>
            <button type="button" class="back-button" onclick="window.location.href='/api/medecins/Listmedecins'">Retour à la liste</button>
        </form>
    </div>

</body>
</html>
