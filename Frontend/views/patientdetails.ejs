<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Détails du Patient</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #f4f4f9; padding: 20px; }
        h1 { color: #6a5acd; }
    </style>
</head>
<body>
    <h1>Détails du Patient</h1>
    <p><strong>Nom :</strong> <%= patient.nom %></p>
    <p><strong>Prénom :</strong> <%= patient.prenom %></p>
    <p><strong>Date de Naissance :</strong> <%= new Date(patient.dateNaissance).toLocaleDateString() %></p>
    <p><strong>Email :</strong> <%= patient.email %></p>

    <h2>Examens</h2>
    <ul>
        <% patient.examens.forEach(examen => { %>
            <li><strong><%= examen.typeExamen %></strong> - <%= new Date(examen.dateExamen).toLocaleString() %></li>
        <% }) %>
    </ul>

    <a href="/api/patient/patients">Retour à la liste</a>
</body>
</html>
